работа хорошего студента.

Просто замени свой README.md на этот код
# Лабораторная работа №2
## Основы работы с массивами, функциями и объектами в JavaScript

---

##  Цель работы

Изучить работу с массивами, объектами и функциями в JavaScript.  
Научиться использовать методы массивов для анализа данных.

---

##  Описание задания

Разработать консольное приложение для анализа транзакционных данных.

Каждая транзакция содержит следующие поля:

- transaction_id — уникальный идентификатор транзакции  
- transaction_date — дата транзакции  
- transaction_amount — сумма транзакции  
- transaction_type — тип транзакции (debit / credit)  
- transaction_description — описание транзакции  
- merchant_name — название магазина  
- card_type — тип банковской карты  

---

##  Структура проекта


Lab2_JS/
│
├── main.js
└── README.md


---

##  Реализованные функции

В работе реализовано 15 функций для анализа транзакционных данных:

- Получение уникальных типов транзакций  
- Подсчет общей суммы транзакций  
- Анализ транзакций по дате  
- Фильтрация транзакций по типу  
- Поиск транзакций в диапазоне дат  
- Поиск транзакций по названию магазина  
- Вычисление средней суммы транзакций  
- Поиск транзакций по диапазону суммы  
- Подсчет общей суммы дебетовых транзакций  
- Определение месяца с максимальным количеством транзакций  
- Определение месяца с максимальным количеством debit транзакций  
- Анализ количества типов транзакций  
- Поиск транзакций до указанной даты  
- Поиск транзакции по ID  
- Получение массива описаний транзакций  

---

##  Пример работы программы

Запуск программы:

```bash
node main.js

Пример вывода в консоль:

Unique types: [ 'debit', 'credit' ]
Total amount: 2450
Average: 816.66
Debit total: 450
🛠 Используемые методы массивов и структуры данных

map()

filter()

reduce()

find()

forEach()

Set()

 Контрольные вопросы
1. Какие методы массивов используются?

Для обработки данных используются:

map() — создание нового массива

filter() — фильтрация элементов

reduce() — сведение массива к одному значению

find() — поиск элемента

forEach() — обход массива

Set() — получение уникальных значений

2. Как сравнивать даты?

Для сравнения дат используется объект Date:

new Date(date1) > new Date(date2)
3. Разница между map, filter и reduce?

map() — создаёт новый массив

filter() — фильтрует элементы массива

reduce() — сводит массив к одному значению

 Вывод

В ходе лабораторной работы были изучены методы работы с массивами и объектами в JavaScript.
Реализовано консольное приложение для анализа транзакционных данных с использованием функций высшего порядка.